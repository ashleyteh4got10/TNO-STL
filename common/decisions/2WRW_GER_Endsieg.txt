GER_2WRW_endsieg = {
    #GER_endsieg_template = {
    #    icon = GFX
#
    #    available = {
    #        AND = {
    #            original_tag = GER
    #            has_war_with = OMS
    #        }
    #    }
#
    #    visible = {
    #        original_tag = GER
    #    }
#
    #    ai_will_do = {
    #        factor = 5
    #    }
#
    #    days_remove = 35
    #    days_re_enable = 200
#
    #    custom_cost_trigger = { command_power > 34.99 }
	#	custom_cost_text = decision_cost_35_cp
#
    #    complete_effect = {
    #        remove_ideas = 
    #    }
#
    #    remove_effect = {
    #        log = "[GetDateText]: [Root.GetName]: Decision "
    #        add_ideas = 
    #    }
    #}

    GER_endsieg_elite_pioniere_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_elite_pioniere_stratagem
                days = 35
            }
            OMS = {
                add_timed_idea = {
                    idea = OMS_elite_pioniere_stratagem
                    days = 35
                }
            }
        }

    }

    GER_endsieg_sondereinsatzkommando_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_sondereinsatzkommando_stratagem
                days = 35
            }
        }
    }

    GER_endsieg_schwerdpunk_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_schwerdpunk_stratagem
                days = 35
            }
        }
    }

    GER_endsieg_feuersturm_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_feuersturm_stratagem
                days = 35
            }
            OMS = {
                add_timed_idea = {
                    idea = OMS_feuersturm_stratagem
                    days = 35
                }
            }
        }
    }

    GER_endsieg_festung_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_festung_stratagem
                days = 35
            }
        }
    }

    GER_endsieg_luftuberlegenheit_stratagem  = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_luftuberlegenheit_stratagem
                days = 35
            }
        }
    }

    GER_endsieg_bewegungskrieg_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_bewegungskrieg_stratagem
                days = 35
            }
        }
    }

    GER_endsieg_verzogerungsgefecht_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_verzogerungsgefecht_stratagem
                days = 35
            }
            OMS = {
                add_timed_idea = {
                    idea = OMS_verzogerungsgefecht_stratagem
                    days = 35
                }
            }
        }
    }

    GER_endsieg_ostlegionen_stratagem = { # 50 misc income to mcw, ukr ost cau. also 10 free stabiltiy and war support perm to add in decision
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }

        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            MCW = { 
                add_timed_idea = {
                    idea = GER_ostlegionen_stratagem
                    days = 35
                }
            }
            UKR = { 
                add_timed_idea = {
                    idea = GER_ostlegionen_stratagem
                    days = 35
                }
            }
            OST = { 
                add_timed_idea = {
                    idea = GER_ostlegionen_stratagem
                    days = 35
                }
            }
            CAU = { 
                add_timed_idea = {
                    idea = GER_ostlegionen_stratagem
                    days = 35
                }
            }
        }
    }

    GER_endsieg_volksturm_stratagem = {
        icon = GFX

        available = {
            AND = {
                original_tag = GER
                GER = {
                    has_war_with = OMS
                }
            }
        }



        visible = {
            original_tag = GER
        }

        ai_will_do = {
            factor = 5
        }

        days_remove = 35
        days_re_enable = 200

        custom_cost_trigger = { command_power > 34.99 }
		custom_cost_text = decision_cost_35_cp

        complete_effect = {
            add_timed_idea = {
                idea = GER_volksturm_stratagem
                days = 35
            }
        }
    }

}


SGR_Zollverein_members_investment_category = {
	SGR_RUSSIAN_WALL_START = {
		icon = GFX_decision_generic_industry
		visible = {
			has_global_flag = german_civil_war_speervic
			date > 1967.1.1
		}

        fire_only_once = yes

		custom_cost_trigger = {
			check_variable = { MemberSpentBudget > 0.200 }
		}

		custom_cost_text = SGR_ZV_debt_reduction_cost


		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision SGR_RUSSIAN_WALL_START"
			subtract_from_variable = { MemberSpentBudget = 0.200 }

		    # Set up everything here for any projects
		    set_variable = { SGR_Project_Progress^SGR_project_start_temp = 5.4 }
		    set_variable = { SGR_Project_Progress_Monthly^SGR_project_start_temp = 2.1 }
		    set_variable = { SGR_Project_Cost_Temp^SGR_project_start_temp = 0.75 }
		    set_variable = { SGR_Project_PP_Cost^SGR_project_start_temp = -0.2 }
		    set_variable = { SGR_Project_PU_Cost^SGR_project_start_temp = -8 }
		    set_variable = { SGR_Project_PU_Investment^SGR_project_start_temp = 8 }
    
		    # Unique
		    set_country_flag = SGR_russian_wall_started
		    set_country_flag = SGR_russian_wall_started_dummy
		    set_variable = { SGR_russian_wall_frame_progress = 1 }
		    add_dynamic_modifier = { modifier = GER_SGR_russian_wall_dynamic_modifier }
    
		    # Efficiency calc
		    set_temp_variable = { SGR_project_calc = RUSSIAN_WALL }
		    SGR_calculate_efficiency = yes
		}

		ai_will_do = {
			factor = 15
		}
	}
}